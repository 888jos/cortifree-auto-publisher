import type { Account, CarouselSpec } from '../domain.js';
export function buildDryRunPayload(spec: CarouselSpec, account: Account, platform: 'tiktok' | 'instagram') { return { dry_run: true, platform, account_id: account.id, profile: account.upload_post_profile || null, carousel_id: spec.id, caption: spec.caption, media: spec.slides.map((slide) => ({ position: slide.position, file: `05_READY_TO_POST/${spec.id}/slide_${String(slide.position).padStart(2, '0')}.png` })), scheduled_at: null, ai_disclosure_mode: process.env.AI_DISCLOSURE_MODE ?? 'auto', idempotency_key: `cortifree:${account.id}:${platform}:${spec.id}` }; }
