import { supabase } from "../../lib/supabase.js";
export async function GET(){try{const r=await supabase("carousels?select=*&order=created_at.desc");return Response.json({carousels:r.ok?await r.json():[]});}catch{return Response.json({carousels:[]});}}
export async function POST(req:Request){const body=await req.json();const id=body.id||`CF_${Date.now()}`;const spec=body.spec||{slides:7};const row={id,account_id:body.account_id||"CF_EN_01",persona_id:body.persona_id||"P01",language:body.language||"en",content_type:body.content_type||spec.carousel_type||"EDUCATIONAL",topic:body.topic||spec.carousel_type_name||"Carousel draft",angle:body.angle||`Draft generated with ${spec.model_id||"selected model"}`,caption:body.caption||"Save this for later.",status:"DRAFT",spec};try{const r=await supabase("carousels",{method:"POST",headers:{Prefer:"return=representation"},body:JSON.stringify(row)});if(!r.ok)throw new Error(await r.text());return Response.json({carousel:(await r.json())[0]||row}, {status:201});}catch(e){return Response.json({carousel:row,mode:"local-fallback",error:String(e)}, {status:201});}}
